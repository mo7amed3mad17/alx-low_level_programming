#include "lists.h"
/**
  * pop_listint - The func to be built
  * @head: The head node
  * Return: int
*/
int pop_listint(listint_t **head)
{
	int i;
	listint_t *Nptr;

	i = head->n;

	Nptr = head;
	head = head->next;
	return (n);
}
